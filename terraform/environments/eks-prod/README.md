# eks-prod

Placeholder for the future `eks-prod` environment.

This environment will follow the same bootstrap contract as `minikube` and
`eks-dev`, but with production-specific provider wiring, state management, and
guardrails.
